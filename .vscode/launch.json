{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug memoria desde raíz",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/memoria",
            "cwd": "${workspaceFolder}"
        }
    ]
}
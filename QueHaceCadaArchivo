si estas medio perdido con que hace cada cosa leete esto (por equipo NutriGO, no por profe, no lo tomen como verdad absoluta)

config.json (Archivo de configuracion): cada modulo tiene uno de estos, estos basicamente almacenan datos utiles para el modulo, en "lenguaje" json.
            por ejemplo puerto : 8080 por decir algo, la ventaja de usarlos es que podes cambiar valores desde aca sin tener que
            recompilar todo. 

globals.go: globals.go se va a encargar de "traducir" los valores que se guardan en el config a un formato .go cosa de que los 
            disintos .go puedan usar estos valores. Lo bueno es que si tenes que cambiar algo, lo cambias solo de un archivo y no
            de todos los que lo estan usando

go.mod: vas a tener un path que basicamente actua de direccion del modulo, cuando quieras acceder a algo que esta en otro modulo
        importas el paquete correspondiente  import pathqueencontrasenelgo.mod/nombredelpackageaimportar (si no queda claro mirar explicacion de "packages")

packages: vas a tener package main o package otra cosa. los package main son lo principal, package otra cosa son helpers al main
            (funciones, variables, etc). como los importamos? vas al go.mod del modulo, copias el path que tenes ahi y pones 
            import pathquecopiaste/package que queres, no podes importar archivos solos, tenes que importar packages, entonces
            si queres importar los valores del global, haces import pathquecopiaste/global //eso es el nombre del package, NO DEL ARCHIVO





